# Third-Party Assets & Code

Every runtime asset or code file not authored in this repo must be recorded here
with source, license, and local path. Nothing is hotlinked at runtime.

## Code templates

| What | Source | License | Local path |
|---|---|---|---|
| Next.js + Phaser React bridge (`PhaserGame.tsx`, `EventBus`, scene skeleton, Next config) | [phaserjs/template-nextjs](https://github.com/phaserjs/template-nextjs) | MIT (Phaser Studio) | `apps/web/src/`, `apps/web/next.config.mjs` (adapted: portrait config, wedding shell) |
| Phaser 4 runtime (`phaser@4.2.1`, npm) | [phaser](https://www.npmjs.com/package/phaser) | MIT (Phaser Studio Inc) | `node_modules` via pnpm (not vendored) |

## World art (M1: generated in-repo, no third-party source)

| What | Source | License | Local path |
|---|---|---|---|
| `wedding-garden` tileset (41 tiles, 16px) + `guest_01` sprite sheet (6x4) + `garden-village-v1` map (56x80, 15 layers) | Generated by `tooling/world-gen/` (repo-owned code, seeded deterministic) | None encumbered (authored in-repo) | `assets-source/{tiled,tilesets,sprites}/`, runtime copy `apps/web/public/assets/worlds/garden-village-v1/` |

## First-party supplied avatar packs (M4.5, NOT third-party)

| Pack | Contents | Local path | Variant |
|---|---|---|---|
| `playable_wedding_avatar_pack_v1` | 4 male guest sheets + metadata | `assets-source/sprites/playable_wedding_avatar_pack_v1/` | source (1536×1024) + runtime (384×256) |
| `playable_wedding_avatar_pack_v2` | 4 female guest + 4 NPC + bride/groom sheets + metadata + role manifest | `assets-source/sprites/playable_wedding_avatar_pack_v2/` | source + runtime |
| `playable_wedding_hijab_pack_v1` | 3 hijabi guest + ivory-hijab bride sheets + metadata | `assets-source/sprites/playable_wedding_hijab_pack_v1/` | source + runtime |
| `playable_wedding_muslim_npc_pack_v1` | greeter/MC/photographer/RSVP NPC sheets + metadata + role manifest | `assets-source/sprites/playable_wedding_muslim_npc_pack_v1/` | source + runtime |

Provenance: artwork supplied directly by the project owner for this project.
Not open-source, not MIT, no public license URL. Do not redistribute outside
this project. Packs are read-only sources; the build copies only the 384×256
runtime sheets to `apps/web/public/assets/avatars/` via the validated
registry (`assets-source/sprites/avatar-registry.json`). Procedural 16×16
NPC placeholders were retired by the same milestone (guest_01 remains as the
legacy fallback sprite only).

## Retired placeholder art

| What | Source | License | Local path |
|---|---|---|---|
| `bg.png`, `logo.png`, `star.png` boot/demo images | phaserjs/template-nextjs (MIT) | MIT | `apps/web/public/assets/` (unused since M1 world scenes replaced the demo) |

## First-party supplied environment pack (M4.6, NOT third-party)

| Pack | Contents | Local path | Variant |
|---|---|---|---|
| `playable_wedding_environment_pack_v2` | 48-tile terrain TSJ, 58 foliage + 83 decor + 10 landmark atlas frames, registries, aliases, collision presets | `assets-source/sprites/playable_wedding_environment_pack_v2/` | source sheets + runtime terrain/atlases |

Provenance: artwork supplied directly by the project owner for this project.
Not open-source, not MIT, no public license URL. Do not redistribute outside
this project. Source sheets stay under assets-source; only terrain PNG/TSJ +
3 atlas PNG/JSON publish to `apps/web/public/assets/environment/`. The chapel
hall exists in-pack but is excluded from default placements by eligibility
metadata (event pavilion serves as the neutral hall via registry alias).

No copyrighted commercial-game art is used. No emoji is used as primary visuals.
